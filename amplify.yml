version: 1
backend:
  phases:
    build:
      commands:
        - echo "backand build........."
        - pwd
        - ls
        - '# Execute Amplify CLI with the helper script'
        - amplify push --y
frontend:
  phases:
    preBuild:
      commands:
        - echo "frontend prebuild..........."
        - pwd
        - ls
    build:
      commands:
        - echo "frontend build..........."
        - pwd
        - ls
        - npm start
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*